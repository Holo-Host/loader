{
  "name": "login",
  "version": "0.0.1",
  "description": "Client side lib injected by holo loader",
  "scripts": {
    "clean": "rm ./dist/*",
    "build": "npm run clean; parcel build ./index.html ./main.js --no-minify --no-content-hash --no-source-maps"
  },
  "author": "",
  "license": "GPL-3",
  "devDependencies": {
    "babel-core": "^6.26.3",
    "babel-plugin-transform-runtime": "^6.23.0",
    "babel-runtime": "^6.26.0",
    "cssnano": "^4.1.10",
    "stylus": "^0.54.5"
  },
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "dialog-polyfill": "^0.4.10",
    "jquery": "^3.3.1"
  }
}
